import React from "react";
import styled from "styled-components";

export default class ViewPort extends React.Component {
  render() {
    return (
      <h1>This will be the rendered map</h1>
    );
  }
}

// Styles
